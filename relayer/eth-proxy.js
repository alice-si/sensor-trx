let EthProxy = {};

EthProxy.saveInfo = function (data) {
    // TODO we need to implement it
    console.log('It should be really cool!!!');
    console.log('We will prcoess the following info in current step');
    console.log(data);
};

module.exports = EthProxy;